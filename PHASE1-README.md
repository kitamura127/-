# Phase 1: CSS変数の導入 - 完了ガイド

## 📦 成果物

Phase 1では以下のファイルを作成しました：

1. **css-variables.css** - CSS変数定義ファイル
2. **phase1-replacement-guide.md** - 手動置き換えガイド
3. **apply-phase1.py** - 自動置き換えスクリプト
4. **PHASE1-README.md** - このファイル

---

## 🚀 使い方

### 方法1: 自動スクリプトを使用（推奨）

#### ステップ1: 元のHTMLファイルを配置
元のHTMLコードを `index.html` として保存してください。

#### ステップ2: スクリプトを実行
```bash
# Pythonスクリプトに実行権限を付与
chmod +x apply-phase1.py

# スクリプトを実行
python3 apply-phase1.py index.html index-phase1.html
```

#### ステップ3: 確認
```bash
# ファイルサイズ確認
ls -lh index*.html

# ブラウザで開く
# index-phase1.html をブラウザで開いて確認
```

---

### 方法2: 手動で置き換え

手動で置き換える場合は、`phase1-replacement-guide.md` を参照してください。

#### ステップ1: CSS変数をコピー
`css-variables.css` の内容を、元のHTMLの `<style>` タグの直後にコピー＆ペースト

#### ステップ2: 検索置換
`phase1-replacement-guide.md` の「適用手順」セクションに従って、エディタの検索置換機能を使用

#### ステップ3: 確認
ブラウザで開いて表示確認

---

## ✅ 動作確認チェックリスト

Phase 1適用後、以下を確認してください：

### 表示確認
- [ ] ページが正しく表示される
- [ ] 色が元のデザインと同じ
- [ ] レイアウトが崩れていない
- [ ] フォントサイズが適切

### 機能確認
- [ ] 左サイドバーの開閉
- [ ] 右サイドバーの開閉
- [ ] タブの切り替え
- [ ] 検索機能
- [ ] マップの操作
- [ ] モバイル表示

### レスポンシブ確認
- [ ] デスクトップ表示（1920x1080）
- [ ] タブレット表示（768x1024）
- [ ] モバイル表示（375x667）

---

## 📊 期待される結果

### ファイルサイズ
- 元のHTML: 約X KB
- Phase 1適用後: 約(X + 6) KB
- CSS変数定義追加分: 約6 KB

### CSS変数使用箇所
- 約200〜300箇所でCSS変数を使用

### メリット
✅ 色・サイズの一元管理が可能に
✅ テーマ変更が容易に
✅ コードの可読性向上
✅ Phase 2以降の基盤が整備された

---

## 🔧 トラブルシューティング

### Q: スクリプト実行時にエラーが出る
**A:** 以下を確認してください：
- Python 3がインストールされているか（`python3 --version`）
- `css-variables.css` が同じディレクトリにあるか
- 入力ファイルが存在するか

### Q: 表示が崩れた
**A:** 以下を確認してください：
- ブラウザのキャッシュをクリア
- Developer Toolsでエラーがないか確認
- CSS変数が正しく定義されているか確認（`:root { ... }`）

### Q: 色が変わってしまった
**A:**
- 特定の色が変わった場合、その色の置換が正しくない可能性があります
- `phase1-replacement-guide.md` の置換マッピング表を確認
- 必要に応じて手動で修正

### Q: 一部のスタイルが適用されない
**A:**
- CSS変数が `:root` 内で定義されているか確認
- 変数名のスペルミスがないか確認
- `var(--変数名)` の形式が正しいか確認

---

## 📝 変更内容の詳細

### 追加されたもの
- CSS変数定義（約200行）
- `:root` スコープ

### 置き換えられたもの
- ハードコードされた色の値
- ハードコードされたサイズの値
- ハードコードされたフォントサイズ
- ハードコードされたborder-radius
- ハードコードされたbox-shadow
- ハードコードされたtransition値

### 変更されないもの
- HTML構造
- JavaScript コード
- クラス名
- ID名
- 機能

---

## 🎯 次のステップ

Phase 1が完了したら、Phase 2に進むことができます。

### Phase 2プレビュー: 共通クラスの作成
- サイドバー用の共通クラス
- ボタン用の共通クラス
- バッジ用の共通クラス
- トグル用の共通クラス

Phase 2は既存コードを変更せず、新しいクラスを追加するだけなので、リスクは低いです。

---

## 📌 注意事項

### Git管理
Phase 1適用後は、必ずGitにコミットしてください：
```bash
git add index-phase1.html
git commit -m "Apply Phase 1: CSS variables"
git push
```

### バックアップ
念のため、元のHTMLファイルは削除せず、Phase 1適用版と並行して保持してください。

### 次のPhaseへ
Phase 1が安定して動作することを確認してから、Phase 2に進んでください。

---

## 📚 参考ドキュメント

- `css-variables.css` - 全CSS変数の定義
- `phase1-replacement-guide.md` - 詳細な置き換えガイド
- `css-refactoring-plan.md` - 全体のリファクタリング計画

---

## ✨ Phase 1完了おめでとうございます！

CSS変数の導入により、コードの保守性が大幅に向上しました。
次のPhaseに進む準備が整いました。

何か問題があれば、`phase1-replacement-guide.md` のトラブルシューティングセクションを参照してください。
